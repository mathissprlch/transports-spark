------------------------------------------------------------------------------
--                                                                          --
--               Generated by RecordFlux 0.26.0 on 2026-05-15               --
--                                                                          --
--                     Copyright (C) 2018-2026 AdaCore                      --
--                                                                          --
--         SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception          --
--                                                                          --
------------------------------------------------------------------------------

pragma Ada_2012;
pragma Style_Checks ("N3aAbCdefhiIklnOprStux");
pragma Warnings (Off, "redundant conversion");
with RFLX.RFLX_Types;

package RFLX.Flow_Gate
with
  SPARK_Mode
is

   type Window_Bytes is range 0 .. 2**31 - 1
   with
     Size =>
       32;

   use type RFLX.RFLX_Types.Base_Integer;

   function Valid_Window_Bytes (Val : RFLX.RFLX_Types.Base_Integer) return Boolean is
     (Val <= 2147483647);

   function To_Base_Integer (Val : RFLX.Flow_Gate.Window_Bytes) return RFLX.RFLX_Types.Base_Integer is
     (RFLX.RFLX_Types.Base_Integer (Val));

   function To_Actual (Val : RFLX.RFLX_Types.Base_Integer) return RFLX.Flow_Gate.Window_Bytes is
     (RFLX.Flow_Gate.Window_Bytes (Val))
   with
     Pre =>
       Valid_Window_Bytes (Val);

   type Op_Kind is (Op_Send_Request, Op_Wu_Conn, Op_Wu_Stream, Op_Init_Stream)
   with
     Size =>
       8;
   for Op_Kind use (Op_Send_Request => 1, Op_Wu_Conn => 2, Op_Wu_Stream => 3, Op_Init_Stream => 4);

   function Valid_Op_Kind (Val : RFLX.RFLX_Types.Base_Integer) return Boolean is
     (Val in 1 | 2 | 3 | 4);

   function To_Base_Integer (Enum : RFLX.Flow_Gate.Op_Kind) return RFLX.RFLX_Types.Base_Integer is
     (case Enum is
          when Op_Send_Request =>
             1,
          when Op_Wu_Conn =>
             2,
          when Op_Wu_Stream =>
             3,
          when Op_Init_Stream =>
             4);

   pragma Warnings (Off, "unreachable branch");

   function To_Actual (Val : RFLX.RFLX_Types.Base_Integer) return RFLX.Flow_Gate.Op_Kind is
     (case Val is
          when 1 =>
             Op_Send_Request,
          when 2 =>
             Op_Wu_Conn,
          when 3 =>
             Op_Wu_Stream,
          when 4 =>
             Op_Init_Stream,
          when others =>
             RFLX.Flow_Gate.Op_Kind'Last)
   with
     Pre =>
       Valid_Op_Kind (Val);

   pragma Warnings (On, "unreachable branch");

   type Decision_Kind is (Dec_Allow, Dec_Deny, Dec_Flow_Error)
   with
     Size =>
       8;
   for Decision_Kind use (Dec_Allow => 1, Dec_Deny => 2, Dec_Flow_Error => 3);

   function Valid_Decision_Kind (Val : RFLX.RFLX_Types.Base_Integer) return Boolean is
     (Val in 1 | 2 | 3);

   function To_Base_Integer (Enum : RFLX.Flow_Gate.Decision_Kind) return RFLX.RFLX_Types.Base_Integer is
     (case Enum is
          when Dec_Allow =>
             1,
          when Dec_Deny =>
             2,
          when Dec_Flow_Error =>
             3);

   pragma Warnings (Off, "unreachable branch");

   function To_Actual (Val : RFLX.RFLX_Types.Base_Integer) return RFLX.Flow_Gate.Decision_Kind is
     (case Val is
          when 1 =>
             Dec_Allow,
          when 2 =>
             Dec_Deny,
          when 3 =>
             Dec_Flow_Error,
          when others =>
             RFLX.Flow_Gate.Decision_Kind'Last)
   with
     Pre =>
       Valid_Decision_Kind (Val);

   pragma Warnings (On, "unreachable branch");

end RFLX.Flow_Gate;
