package body Codegen.Driver is

   ---------
   -- Run --

   procedure Run is
   begin
      --  Skeleton. The plugin contract is "read CodeGeneratorRequest from
      --  stdin, write CodeGeneratorResponse to stdout"; an empty response
      --  is zero bytes, which means "I produced no files, no error."
      --  The actual stdin parser and codegen land in the next commits.
      null;
   end Run;

end Codegen.Driver;
