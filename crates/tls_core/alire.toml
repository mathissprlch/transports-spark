name = "tls_core"
description = "Pure Ada/SPARK TLS 1.3 with platinum proofs (miTLS-informed)"
version = "0.0.1"

authors = ["Mathis Sperlich"]
maintainers = ["Mathis Sperlich <sperlich.mathis@gmail.com>"]
maintainers-logins = ["mathis"]
licenses = "MIT"
tags = ["tls", "spark", "verified", "mitls"]

project-files = ["tls_core.gpr"]
