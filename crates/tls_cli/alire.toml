name = "tls_cli"
description = "Standalone TLS 1.3 CLI for the tls_interop matrix (no AWS dep)"
version = "0.0.1"
licenses = "MIT OR Apache-2.0 WITH LLVM-exception"
authors = ["Mathis Sperlich"]
maintainers = ["Mathis Sperlich <sperlich.mathis@gmail.com>"]
maintainers-logins = ["mathis"]

project-files = ["tls_cli.gpr"]

[[depends-on]]
tls_core = "*"

[[pins]]
tls_core = { path = "../tls_core" }
