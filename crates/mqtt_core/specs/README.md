# MQTT 3.1.1 — RecordFlux specifications

`.rflx` files describing MQTT 3.1.1 packet layouts and the client
session state machine. Compiled to SPARK Ada under `../generated/`
by `rflx generate`.

Reference: MQTT 3.1.1 spec, OASIS Standard.
