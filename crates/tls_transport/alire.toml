name = "tls_transport"
description = "TLS 1.3 transport adapter, same API as tcp/bare Transport"
version = "0.5.0"
authors = ["Mathis Sperlich"]
licenses = "MIT OR Apache-2.0 WITH LLVM-exception"

[[depends-on]]
gnat = ">=12"
