# v0.5 Tier D bug log

Every bug surfaced by external interop testing — its RFC citation,
classification per docs/conventions.md §9, and fix commit. Per docs/conventions.md §9a,
this log is a **release artifact**: missing rows = incomplete
bookkeeping, and the (d) tags are the v0.5 honest-reporting caveat
for primitives shipped as `[VERIFIED — AoRTE]` whose functional
Post was insufficient (found-by-interop, not by gnatprove).

| Date | Found by | Component | RFC § | Tag(s) | One-line summary | Commit |
|---|---|---|---|---|---|---|
| 2026-05-08 | openssl `tls_process_client_hello` | `Tls_Core.Hello.{Encode_Client_Hello_Psk, Encode_Client_Hello_Cert}` | RFC 8446 §4.4.1 | (a) | PSK binder hash covered the wrong byte range — included the binder length prefix when it should have stopped at `.identities`. | b7dc221 |
| 2026-05-08 | openssl `tls_process_server_hello` | `Tls_Core.Hello.Decode_Server_Hello_Psk_Key_Share` | RFC 8446 §4.2.8.1 | (a) | CH key_share length was 0x26 (38) — should be 0x24 (36) per §4.2.8.1; the u16 prefix excludes itself. | aefd98d |
| 2026-05-08 | openssl `s_client` | `Tls_Core.Tls13_Driver.Init_Psk_Client` | RFC 8446 §7.1 | (a) | binder_key derivation invoked HKDF-Expand-Label with empty Context; spec wants `Derive-Secret` which hashes Messages first. | aefd98d |
| 2026-05-08 | openssl handshake reassembly | `Tls_Core.Tls13_Driver.Hs_Trunc` | RFC 8446 §5.1 | (c)+(d) | 1024-byte transcript-truncation buffer too small for a real openssl 1487-byte ClientHello; bumped to 16640. AoRTE proved buffer wasn't read past `Last`; no functional Post said it must hold a full CH. | 6fb7ed0 |
| 2026-05-08 | openssl + mbedtls `tls_process_client_hello` | `Tls_Core.Hello.{Decode_Cert_Verify, Decode_Body_Single}` | RFC 8446 §4.2.3 | (c)+(d) | `Decode_Body_Single`'s consistency check `1+3+Cursor-4 /= List_Len` algebraically simplified to `Cursor /= List_Len` — off by 4. Pure transcription error. Post didn't bind decoded bytes to encoded bytes, so a `Spec_Decode_Single` round-trip Post would have caught this. Latent until D-4-D's client cert receive made it the first call site. | 22cc27f |
| 2026-05-09 | openssl `tls_process_server_hello: invalid session id` | `Tls_Core.Hello.{Encode_Server_Hello_Psk, Encode_Server_Hello_Cert}` | RFC 8446 §4.1.3 | (a)+(d) | server hard-coded `legacy_session_id_echo = empty`; openssl/mbedtls clients abort on mismatch (gnutls passes — its client sends empty session_id). AoRTE Post said "produces ≥40 bytes shaped like SH"; functional Post `serverHelloMsg.legacy_session_id_echo == client.legacy_session_id` tied to miTLS would have rejected the encoder at gnatprove time. | (WS1) |
| 2026-05-09 | tls_core_tests range-check exception | `Tls_Core.Tls13_Driver` (driver session_id capture) | RFC 8446 §4.1.3 | (c) | guard `Sid_L >= Sid_F` matched both-zeros (empty session_id) and tried to slice `In_Bytes (0 .. 0)`; tightened to `Sid_F > 0 and then Sid_L >= Sid_F`. Not a spec issue — the captured-from-decoder branching forgot the empty-session case the decoder zeroes out. | (WS1) |
| 2026-05-09 | openssl `final_sig_algs: missing sigalgs extension` | `Tls_Core.Hello.Encode_Client_Hello_Psk` | RFC 8446 §4.2.3 + §9.2 | (a) | PSK ClientHello omitted `signature_algorithms`. RFC 8446 §9.2 requires it in every CH (including resumption-PSK CHs), even though PSK auth doesn't use cert signatures. openssl rejects the CH outright. Encoder now emits sig_algs (ecdsa_secp256r1_sha256 + rsa_pss_rsae_sha256) before key_share. | (WS3) |
| 2026-05-09 | constraint-error on Slot.Ticket length 192 vs cap 64 | `Tls_Core.Tls13_Driver.Init_Psk_Resumption_Client` Pre + `Identity_Bytes` subtype | RFC 8446 §4.2.11 | (a) | v0.5 cap was 64-byte identity; production NewSessionTicket bytes are larger (openssl ≈190 B, gnutls/bssl can push >256 B). Lifted `Psk_Identity_Len` and `Identity_Bytes` to 1..1024, matching driver-internal envelope. The cap was a known v0.5-cap note that needed lifting before resumption interop. | (WS3) |
| 2026-05-09 | constraint-error: ECDHE shared = bogus (zeros) | `Tls_Core.Tls13_Driver.Init_Psk_Resumption_Client` body | RFC 8446 §4.2.11.2 | (b) | Init_Psk_Resumption_Client called Prime_Driver_Defaults but never called X25519.Derive_Public to populate `My_Ecdhe_Priv` / `My_Ecdhe_Pub`. The CH was emitted with a zero pubkey; openssl's ECDHE derivation failed. Now derives ephemeral from `Slot.Resumption_Secret` (each session has a unique secret). Spec-wise this matches §4.2.11.2 mode-3 "psk_dhe_ke" — the encoder always advertised mode 3, but the body wasn't actually generating an ECDHE key. | (WS3) |
| 2026-05-09 | tls-interop psk-external-chacha20 c2s | `Tls_Core.Key_Sched.Derive_Handshake_Secrets` | RFC 8446 §7.1 | (a) | HKDF-Extract Salt/IKM swapped in Key_Sched: `Extract(Salt => PSK, IKM => 0)` instead of `Extract(Salt => 0, IKM => PSK)`. Produced wrong Early_Secret for non-zero PSK, causing `bad_record_mac` on all external-PSK handshakes. Cert-mode unaffected (PSK = 0 makes Salt and IKM interchangeable). Introduced in e437d27 (inline→Key_Sched refactor), found by bisecting matrix regression. | f7036b5 |
| 2026-05-09 | tls-interop psk s2c openssl + gnutls cert-ec s2c | `Tls_Core.Tls13_Driver.Step_Awaiting_Ch` binder verify | RFC 8446 §4.2.11.2 | (c)+(d) | Uninitialized `Received : Binder_Bytes` local (now 48 bytes after widening) — bytes 33-48 contained garbage, causing `Verify` to return False on valid binders. Pure impl bug; a `Default_Value` aspect or flow-analysis warning would have caught it. | (v0.5.1) |
| 2026-05-09 | tls-interop gnutls cert-ec s2c | `Tls_Core.Tls13_Driver.Step_Awaiting_Ch` cert-mode X25519 copy | RFC 8446 §4.2.8 | (c) | Batch sed replaced `for I in 1 .. 32` with `Hash_Len(D.Suite)` in the X25519 key_share copy loop. X25519 keys are always 32 bytes regardless of hash; when AES-256 was in scope Hash_Len returned 48, reading 16 bytes past the key. | (v0.5.1) |
| 2026-05-11 | manual openssl + gnutls cert-ec s2c (TLS_AES_256_GCM_SHA384) | `Tls_Core.Tls13_Driver.Step_Awaiting_Ch_Cert` CV signing | RFC 8446 §4.4.3 | (a)+(d) | Server-side CertificateVerify signed the first 32 bytes of `Th_After_Cert` (hardcoded `(1 .. 32)`) instead of `(1 .. Hash_Len(D.Suite))`. For SHA-384 cipher suites the transcript hash is 48 bytes, so the signature covered a truncated hash — peers correctly rejected as bad signature. Matrix masked the bug because it forced `-ciphersuites TLS_CHACHA20_POLY1305_SHA256` (SHA-256). Same "SHA-256 baked in" class as bug #4 in the pattern table. Functional Post `Signed_Content = Verify_Prefix \|\| Th_After_Cert(1..Hash_Len(Suite))` would have caught it. | (this commit) |
| 2026-05-15 | §7 audit: production driver missing cert-mode dispatch | `Tls_Core.Tls13_Driver.Step` case statement | RFC 8446 §4.4.2 + §4.4.3 | (b) | Init_Cert_Server / Init_Cert_Client set `D.Mode := Cert_Mode` and the sibling Step_Awaiting_{Ch,Sf}_Cert handlers existed in the same package, but Step's case statement always dispatched to the PSK-mode handlers regardless of `D.Mode`. Net effect: cert-mode appeared "C8 done" per the task list (entry points + sub-handlers landed) but no caller could actually drive a cert-mode handshake through Step — exactly the §7 failure mode ("sibling package primitives ≠ production-driver integration"). Fix: added the `if D.Mode = Cert_Mode then Step_Awaiting_*_Cert.Handle else Step_Awaiting_*.Handle` branch to Awaiting_CH (server) and Awaiting_Sf (client). Spec mirror cited in the comment: miTLS `src/tls/MiTLS.Handshake.Server.fst : serverHandshakeStep` (two-axis dispatch by state × mode). Full ECDSA-P256 cert-mode loopback (CH → SH+EE+Cert+CV+SF → CF → app-data round-trip) now passes; 598/598 tests green. RSA-PSS server-side signing remains the known follow-up gap (chain validation is verify-only). | (D-4) |
| 2026-05-15 | wolfSSL cert-ec interop deferred status outdated | `Tls_Interop_Peers.Build_Wolfssl` (matrix peer note only — no Ada code change) | RFC 6125 §6.4 (hostname matching) | (e) | The "wolfSSL cert-mode flight decode mismatch in both directions" note was stale — that bug class is gone after D-4 + earlier WS fixes. Verified end-to-end: Ada client vs `wolfssl examples/server/server -v 4 -c server-ecc.pem -k ecc-key.pem` reaches Done cleanly for both AES_256_GCM_SHA384 (first external SHA-384 path exercised) and CHACHA20_POLY1305_SHA256, with `cv auth result=OK_VALIDATED`. Only remaining gap: wolfSSL's stock test fixture `certs/server-ecc.pem` has NO X509 SubjectAltName extension (`openssl x509 -ext subjectAltName` reports "No extensions in certificate") so our (correct, RFC 6125-compliant) hostname check rejects with `Bad_Signature`. Classification (e): counterpart fixture below modern TLS bar — not an Ada-side bug. Matrix note updated to describe the actual gap; lifting the cell to PASS requires either regenerating a SAN-bearing leaf signed by wolfSSL's CA or using `certs/server-ecc-comp.pem` (compressed-point fixture, has `DNS:example.com` SAN). | (#139) |
| 2026-05-15 | bssl c2s interop deferred status outdated | `Tls_Interop_Peers.Build_Boringssl` (matrix peer note only — no Ada code change) | n/a (bssl example-server behaviour) | (e) | Old note claimed bssl emits half-RTT NST coalesced into the SF record; real observation differs. `bssl server -debug` shows the handshake state machine walking the full flight (read_client_hello → send_server_hello → send_server_certificate_verify → send_server_finished → send_half_rtt_ticket → read_second_client_flight → ...) yet `netstat -an` reports TCP send queues at 0 bytes in BOTH directions during the entire stall. Same Ada client succeeds end-to-end against openssl/gnutls/mbedtls/wolfssl on identical fixtures, so this is bssl-specific. Hypothesis: bssl's example tool buffers the flight at the BIO layer pending a synchronous app-data write that never comes from our client; the `-debug` progress callbacks track state-machine transitions, not actual socket writes. Lift this NI-3P only when (a) bssl exposes a flush flag, or (b) we wire a different bssl-based server (Cronet / Chromium net stack). bssl s2c (Ada server) DOES work and is exercised in the matrix. | (#137) |
| 2026-05-15 | Cert-mode CH missing `psk_key_exchange_modes` extension | `Tls_Core.Hello.Encode_Client_Hello_Cert` | RFC 8446 §4.2.9 (literal) + docs/conventions.md §0a (production shape) | (a) | RFC 8446 §4.2.9 says `psk_key_exchange_modes` "MUST be included when offering PSK" — read literally that's only required in PSK CHs. But every production TLS 1.3 client (openssl s_client, BoringSSL bssl client, gnutls-cli, Go crypto/tls, Chromium net stack) emits this extension unconditionally as part of the production CH shape. Our cert-mode CH omitted it (an explicit design choice per the encoder comment). Net effect: our CH was RFC-conformant but didn't match production-default shape, leaving a subtle interop fingerprint. Now emitted unconditionally (modes_len=1, mode=psk_dhe_ke). 6 extra bytes on the wire; no protocol effect when no PSK is offered. Investigated as the suspected bssl-stall root cause (it wasn't — see entry above), but kept anyway because matching production shape is the right §0a posture. 598/598 tests green; matrix outcomes unchanged across all working peers. | (this commit) |

## Reading the log

- **Tag (a) ratio**: 6 of 8 entries are pure spec misreads. Mirrors
  docs/conventions.md §9's "(a) is the dominant failure mode in TLS work."
  Mitigation per §5: mirror miTLS / HACL* spec structure rather than
  re-reading the RFC each time.
- **Tag (d) ratio**: 4 of 7 entries are paired with a proof gap.
  Each one is a primitive whose `[VERIFIED — AoRTE]` claim was
  honest about runtime safety but silent about functional
  correctness. Per §0d.5 these are NOT platinum until the Post is
  closed against a real spec mirror.
- **Tag (e) ratio**: 0. No bugs traced to peer non-conformance.
  Every fail was ours.

## Implications for v0.5 release report

The v0.5 platinum claim is qualified by:

- The 4 (d) entries above — primitives where AoRTE was insufficient.
  v0.6 ghost-bignum work + Post tightening will move them from
  AoRTE → PLATINUM.
- The 1 unproved VC in `Tls_Core.Poly1305.Mac` (docs/conventions.md §0e wall).

Together: **5 functional contracts where a stronger spec-tied Post
should have caught the bug or remains open**. The honest summary
reads "TLS 1.3 implementation passes 6-peer interop matrix; 4 of N
crypto primitives' functional Posts are AoRTE-only and would
benefit from spec-mirror Post tightening; 1 functional VC is
blocked on the SPARK-stdlib Big_Integer-bridge wall."

This is the language that goes in the v0.5 release notes — NOT
"100% proven."

| 2026-05-10 | tls-interop gnutls s2c | `Tls_Core.Tls13_Driver` (server) | RFC 8446 §4.1.3 / §7.1 | TBD | gnutls-cli reports "invalid decryption" on first encrypted record from Ada server; key schedule mismatch. x25519-only priority still fails. openssl/mbedtls/go/rustls/bssl s2c all PASS — gnutls-specific. Needs wire-level key comparison. | (open) |

## SHA-384 cert-mode hardcoded-32 bugs (2026-05-11)

**Found by:** MQTT-over-TLS against Mosquitto (AES-256-GCM-SHA384)
**Component:** `step_awaiting_sf_cert.adb` + `key_sched.adb` + `tls13_driver.adb`
**RFC §:** 4.4.3 (CertificateVerify), 4.4.4 (Finished), 4.4.1 (transcript), 7.1 (key schedule)

### Root cause analysis

All five bugs share one root cause: **the cert-mode client path was developed and tested exclusively with ChaCha20-Poly1305-SHA256 (our Ada-to-Ada interop certs).** The SHA-384 code paths existed (dispatching on Suite), but the values fed to them were hardcoded to 32 (SHA-256 hash length) in the calling driver code. No SHA-384 server was ever tested against until Mosquitto.

This is root cause **(a) spec misread + (c) impl bug + (d) proof gap** per §9:
- **(a)**: RFC 8446 §4.4.3/§4.4.4/§7.1 all say "Hash.length" (suite-dependent), not "32"
- **(c)**: Five locations used literal `32` where `Hash_Len(D.Suite)` was needed
- **(d)**: No functional Post on the driver's handshake step binds the transcript hash length to the suite; a Post saying `Th_After_Cert'Length = Hash_Len(Suite)` would have caught all five at prove time

### Individual bugs

| # | Location | What was hardcoded | Fix | How a proof would have caught it |
|---|---|---|---|---|
| 1 | `x25519 key copy loop` | `Hash_Len(Suite)` used for 32-byte x25519 key | `1 .. 32` | Loop bound Post vs `Peer_Pub'Length` |
| 2 | `Transcript_Append` | Only appended to one hash context based on Suite (default=SHA256) | Always append to both | Dual-context invariant |
| 3 | `Init_Cert_Client` | Only initialized `Hash_Ctx` (SHA-256), not `Hash_Ctx_384` | Init both | Init completeness Post |
| 4 | `CertVerify transcript hash` | `Th_After_Cert(1..32)` | `Th_After_Cert(1..Hash_Len(Suite))` | Post binding transcript slice to suite |
| 5 | `ServerFinished length check` | `Msg_Last /= 4 + 32` | `Msg_Last /= 4 + Hash_Len(Suite)` | Post on expected SF length |
| 6 | `SF MAC loop invariant` | `I in 1..32` | `I in 1..48` | Invariant matching Hash_Len range |

### Prevention

The class of bug is "SHA-256 baked in as the only hash." Prevention:
1. **grep for literal `32` in driver code** — any `32` that represents a hash length should be `Hash_Len(Suite)` or `HL`/`HH` constant
2. **Test against a SHA-384 server** before claiming cipher-suite negotiation works
3. **Add functional Posts** on Step that bind hash-dependent values to `Hash_Len(Suite)`

### Also fixed in same session
- MQTT transport: IP literal hostname skip (same pattern as HTTP/2)
- Logger crate: shared logging with Enable_Logging for release stripping
- docs/conventions.md §14: mandatory logging rule for all new features
