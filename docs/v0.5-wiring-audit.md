# v0.5 wiring audit — features claimed "complete" vs actual driver integration

**Framing (per CLAUDE.md §8):** every gap below is first a deviation
from a specific RFC 8446 paragraph. Peer rejection (openssl, rustls,
etc.) is a *consequence* of being off-spec, not the *reason* to fix.
The bar is "we satisfy the §X.Y text", not "openssl accepts".

**Trigger:** 2026-05-08, Tier D (external interop) discovered cert-mode
landed in `Tls_Core.Handshake_Driver` (an older, pre-record-layer
driver) but was never integrated into `Tls_Core.Tls13_Driver` (the
production record-layer-aware driver). Two task-list entries (C8
"cert-mode driver end-to-end against real PKI"; Phase 14 "Real Cert
+ CertVerify with full DER chain") were marked completed despite
the gap. This document audits every C/P-track feature with the same
question pattern.

**Audit pattern per feature:** for each "completed" feature, three
things must be true. If any is missing, the feature is not actually
integrated for the purposes of external interop.

| Check | What it proves |
|---|---|
| (a) Public entry point in `Tls13_Driver.ads` | The advertised API surface is callable |
| (b) `Tls13_Driver.Step` references the feature | The state machine actually exercises it |
| (c) A driver-level test (not just primitive-pkg test) | We've executed the path end-to-end |

## Audit matrix — current state on `main`

| Track | Feature | (a) Public ADS entry | (b) Step references | (c) Driver test | Status |
|---|---|---|---|---|---|
| C7 | psk_dhe_ke (mode 3) | `Init_Psk_*` w/ `Ecdhe_Priv` | yes (key-share parse + ECDHE secret extract) | `Tls13_Mode3_Ecdhe_Contributes` | **WIRED** |
| C8 | cert-mode handshake | **MISSING** — no `Init_Cert_*` in `Tls13_Driver.ads` | **NO** — no Cert / CertVerify decode in Step | only `Authenticate_Server` primitive test | **GAP** (D-4) |
| C9 | cipher-suite negotiation | `Selected_Suite (D)` exposes choice | yes — server picks per code at lines 514 / 926 | `Driver_Loopback_Scenario` × 3 suites | **WIRED** |
| C10 | alert protocol | `Send_Close_Notify`, `Send_Fatal_Alert`, `Last_Alert_Description` | partial — only 4 Step error paths emit (Awaiting_CH bad header, Awaiting_Sf bad MAC EE / decode-after-OK EE / bad MAC SF / decode-after-OK SF). Many other `D.Cur_State := Failed; return;` paths still don't emit. | `Alert_Bad_Record_Mac_Scenario` + `Alert_Decode_Error_Scenario` + `Alert_Plaintext_Fatal_Scenario` | **PARTIAL** — coverage gap, not a foundation gap |
| C11 | HelloRetryRequest | `Init_Psk_Server_With_Hrr`, `Init_Psk_Client_Hrr_Aware` | yes — `D.Hrr_*` state read+set in Step server CH-receive branch (lines 1027-1048) and client SH-receive | `Tls13_Hrr_Loopback` + `Hello_Retry_Unit` | **WIRED** |
| C12 | multi-record reassembly | `Hs_In_Buf` field on Driver | partial — `Push_Record_Bytes` / `Pop_Complete_Message` used only in client-side `Awaiting_Sf` (lines 668-693). Server-side `Awaiting_CH` and `Awaiting_Cf` paths still parse one record == one message. | `Tls13_Multi_Record_Reassembly_Scenario` | **PARTIAL** — works for client receiving server flight; server-side incomplete |
| C13 | NewSessionTicket | `Send_New_Session_Ticket` (server, manual), `Receive_New_Session_Ticket` (client, manual), `Init_Psk_Resumption_Client` | NO Step integration — both Send / Receive are explicit caller calls | `Session_Ticket_Wire_Scenario` + `Session_Cache_Scenario` + `Session_Ticket_End_To_End_Scenario` | **WIRED for explicit-call use** — Step does NOT auto-process inbound NST records during app-data flow. **Real interop gap:** an openssl server emits NST in the encrypted app-data stream right after Done; if our driver doesn't auto-route inbound 0x04 (NewSessionTicket) handshake messages out of the encrypted record stream, we'll fail or drop them. |
| C14 | KeyUpdate | `Send_Key_Update` (manual), `Process_Inbound_Key_Update` (manual) | NO Step integration | direct unit test for the Send / Process calls | **WIRED for explicit-call use** — same caveat as C13. RFC 8446 §4.6.3 requires processing inbound KeyUpdate; if Step doesn't auto-route inbound 0x18 (KeyUpdate) messages, peer-initiated rotation will break us. |
| C15 | mode-1 demote | n/a (negative — verify removal) | yes — only `psk_dhe_ke` advertised in CH | round-trip tests don't exercise mode 1 | **DONE** |
| Phase 12+13 | ALPN | **MISSING** — no `Set_Alpn_Offers` / `Selected_Alpn` / `Alpn_*` in `Tls13_Driver.ads` | **NO** — no `Alpn` references in driver body | **ORPHAN** — `Tls_Core.Extensions.Encode_Alpn` / `Decode_Alpn` exist (RFC 7301 wire format, ~30 LOC body) but no `with` clauses or callers anywhere. Hello.adb's CH/SH/EE encoders never include the ALPN extension. | **WIRE-FORMAT ORPHAN** — primitives there but disconnected from CH emit. Half-day to wire. |
| Phase 12+13 | SNI | **MISSING** — no `Set_Sni_Hostname` / `Server_Name` in `Tls13_Driver.ads` | **NO** — no `Sni` / `Server_Name` references in driver body | **ORPHAN** — `Tls_Core.Extensions.Encode_Server_Name` / `Decode_Server_Name` exist (RFC 6066 §3 wire format) but disconnected like ALPN. | **WIRE-FORMAT ORPHAN** — half-day to wire. |
| Tier P-RSA | RSA-PSS sign | **MISSING** — `Tls_Core.Rsa_Pss` exposes only `Emsa_Pss_Verify_Sha256/384` | n/a | n/a | **VERIFY-ONLY** — sign primitive missing. Limits server-side cert-mode to ECDSA-P256 leaves. |
| D-1 | PSK binder hash scope | exists (`Psk_Binder.Compute`) | called from Step | covered by PSK loopback tests | **WRONG SCOPE** — hashes the body, RFC 8446 §4.4.1 wants the handshake-message-formatted CH (4-byte type+length prefix included). Ada-vs-Ada agreed-on-wrong-hash; openssl rejects. |

## Feature-by-feature plan to close the gaps

### Tier 1 — must close for v0.5 release (production interop)

1. **D-4: Cert-mode in `Tls13_Driver`** — biggest piece.
   - Add `Init_Cert_Server (D, Cert_Chain, Sign_Algo, Priv_Key)` and
     `Init_Cert_Client (D, Server_Name, Trust_Anchors)` to
     `Tls13_Driver.ads`.
   - Extend `State` enum: client adds `Awaiting_Cert`, `Awaiting_CertVerify`
     between `Awaiting_Sf` (currently EE→SF) so the order is
     EE → Cert → CertVerify → SF. Server side adds the symmetric
     emit path.
   - Add Cert + CertVerify handshake-message types: 0x0B, 0x0F.
   - Server emit: build §4.4.2 Certificate (0x0B + uint24 +
     certificate_request_context (empty) + certificate_list
     (CertificateEntry list of length-prefixed DER + extensions
     (empty))) → encrypt under handshake key. Then build §4.4.3
     CertificateVerify (0x0F + uint24 + sig-algo (rsa_pss_rsae_sha256
     or ecdsa_secp256r1_sha256) + signature length-prefixed). The
     signature input is `0x20 × 64 || "TLS 1.3, server CertificateVerify"
     || 0x00 || transcript-hash-up-to-Cert`.
   - Client receive: pop Cert message → parse certificate_list →
     `Cert_Verify.Authenticate_Server` against the trust anchors.
     Pop CertVerify → reconstruct the 64-space-prefixed input →
     verify signature using the server's leaf pubkey.
   - **Server-side RSA-PSS sign is NOT implemented** (we have only
     verify in `Tls_Core.Rsa_Pss`). Server-side cert-mode in v0.5
     limited to ECDSA-P256 leaves; RSA-PSS server is a deferred gap
     to flag.

2. **D-1: Fix PSK binder hash scope.**
   - 3 call-sites in `tls13_driver.adb` (lines ~399, ~979, ~1504)
     pass `Ch_Body (1 .. T_Last)`. They must instead pass the
     handshake-message-formatted bytes (`0x01 || uint24(len) ||
     body`) to `Psk_Binder.Compute`.
   - Or, simpler: change `Psk_Binder.Compute` to take the body and
     internally prepend the handshake header to the hash input.
     Either works; the second keeps the call-sites tidy.

3. **C13 / C14 inbound auto-routing.**
   - Step must, after Done, demux inbound app-data records: if the
     decrypted `Inner_Type == 0x16` (Handshake), call into a
     post-handshake demux that recognises:
     - 0x04 NewSessionTicket → `Receive_New_Session_Ticket` body
     - 0x18 KeyUpdate → `Process_Inbound_Key_Update` body
     Otherwise leave the bytes untouched on `Out_Buf`.
   - Otherwise: openssl emits NST 250-1500ms after Done as part
     of normal flow; we'll mishandle it.

### Tier 2 — should close for v0.5 release (real-world handshake completeness)

4. **ALPN — RFC 7301: wire the orphan codec into CH/EE emit.**
   - `Tls_Core.Extensions.Encode_Alpn` already exists; just unused.
   - Hello.adb: add `Ext_ALPN (0x0010)` to the in-CH emit path
     (using Extensions.Encode_Alpn for the body) and to the EE emit
     path on the server side per RFC 8446 §4.3.1.
   - Hello.adb: parse ALPN extension from inbound CH (server) and
     EE (client).
   - Driver: `Set_Alpn_Offers (D, Offers)` + `Selected_Alpn (D)
     return Alpn_String`.
   - **Effort revised: half-day** (wire format primitives exist).

5. **SNI — RFC 6066 §3: wire the orphan codec into CH emit.**
   - `Tls_Core.Extensions.Encode_Server_Name` / `Decode_Server_Name`
     exist; unused.
   - Hello.adb: add `Ext_Server_Name (0x0000)` to in-CH emit path.
     Optional server-side decode + driver-accessor.
   - Driver: `Set_Sni_Hostname (D, Hostname)` client-side.
   - **Effort revised: half-day** (wire format primitives exist).

6. **C12 server-side reassembly.**
   - Mirror the client-side `Push_Record_Bytes` / `Pop_Complete_Message`
     loop on the server's `Awaiting_CH` and `Awaiting_Cf` paths.
     Required if a client splits their CH or sends Cert chain
     (when we add client-cert support — out of v0.5 scope but
     foundation).

7. **C10 broader alert wiring.**
   - Audit all `D.Cur_State := Failed; return;` paths in Step;
     classify by RFC 8446 §6.2 description (decode_error,
     unexpected_message, illegal_parameter, bad_record_mac,
     handshake_failure, etc.). Wire each to either `Fail_Plaintext`
     or `Fail_Encrypted` per the keys-derived state. Currently
     only ~5 of ~30 such paths emit alerts.

### Tier 3 — defer to v0.5.x or v0.6 (limitations to document, not block on)

8. **Server-side RSA-PSS sign primitive.** Add `Tls_Core.Rsa_Pss.Sign`
   (FIPS 186-4 / RFC 8017 §8.1.1). Required to be an RSA-PSS server;
   server-side cert mode without it is ECDSA-P256 only.

9. **Resumption flow closure (C13 part 2).** With cert-mode and
   `Init_Psk_Resumption_Client` both landed, the resumed handshake
   path needs end-to-end testing; currently the resumption client
   is initialisable but a full cert-mode → NST → resumption →
   psk_dhe_ke flow has not been driven through Step.

10. **0-RTT / early data.** Out of scope per CLAUDE.md §0a.

11. **Server-side cert-mode RSA-PSS leaf** (depends on item 8).

## Audit-runner shell snippet

To re-audit on demand:

```sh
# (a) Public entry points expected by the C-track:
grep -nE "Init_Cert_|Init_Psk_|Set_Alpn_Offers|Set_Sni_Hostname|\
Selected_Alpn|Send_New_Session_Ticket|Send_Key_Update" \
  crates/tls_core/src/tls_core-tls13_driver.ads

# (b) Step-side dispatching:
grep -nE "Hs_Type_Cert\b|Hs_Type_CertVerify\b|Hs_Type_NewSessionTicket\b|\
Hs_Type_Key_Update\b|Alpn|Server_Name" \
  crates/tls_core/src/tls_core-tls13_driver.adb

# (c) Driver-level tests (not primitive-only):
grep -nE "Tls13_.*_Scenario" crates/tls_core/tests/src/tls_core_tests.adb
```

A feature with all three returning hits is integrated. Anything
missing from (a) or (b) is a potential gap; anything in (c) without
matching (a)/(b) entries is a primitive-test masquerading as a
driver test.

## Known wiring gaps after Tier D phase 1 (2026-05-08)

| Tag | Severity | Remediation effort | Blocks v0.5 release? |
|---|---|---|---|
| D-4 (cert-mode) | **CRITICAL** | 2-4 days (driver-level wiring; X.509 + verify primitives already exist) | **YES** |
| D-1 (binder hash scope) | high | DONE (commit 9a64055) | n/a |
| C13 inbound auto-route | high | half day | yes (NST always emitted by openssl server) |
| C14 inbound auto-route | high | half day | yes (RFC mandatory) |
| ALPN (RFC 7301) — wire orphan codec into CH/EE | medium | half day (Extensions.Encode_Alpn exists, just unused) | depends on use case (gRPC = yes, raw TCP = no) |
| SNI (RFC 6066 §3) — wire orphan codec into CH | medium | half day (Extensions.Encode_Server_Name exists, just unused) | depends on peer config (virtual hosts require it) |
| C12 server-side | medium | 1 day | only if client splits CH (rare in practice) |
| C10 broader alert wiring | medium | 1 day | improves error reporting; doesn't block happy path |
| RSA-PSS sign primitive | medium | 1-2 days | only blocks RSA-PSS server direction (ECDSA-P256 server still works) |

**Aggregate v0.5 wiring close-out estimate:** 4-8 working days
(D-1 done; cert-mode driver merge is the dominant single task at
2-4 days; everything else is half-day to 1-day each, building on
existing primitive layers).

## Re-audit (round 2) — verification of every "completed" v0.5 task

Triggered 2026-05-08 after surfacing the cert-mode and SNI/ALPN
gaps. Cross-verified all task entries claimed completed in the
v0.5 line by greping the actual codebase (not trusting prior
session reports). Findings beyond what was already documented above:

**Verified WIRED (no gap, callable from production driver):**
- C7 psk_dhe_ke (mode 3) — `Init_Psk_*` w/ `Ecdhe_Priv` + Step
  ECDHE-secret extract
- C9 cipher-suite negotiation — `Selected_Suite (D)` + Step picks
  per code byte
- C11 HelloRetryRequest with §4.4.1 transcript-hash quirk —
  `Synthetic` / `Hrr_Ch1_Hash` / `Build_Synthetic_Msg_Sha256`
  wired in Step at lines 1033/1046/1055
- C13 / C14 explicit-call primitives (`Send_New_Session_Ticket`,
  `Receive_New_Session_Ticket`, `Send_Key_Update`,
  `Process_Inbound_Key_Update`)
- C15 PSK_KE mode 1 demoted (only mode 3 advertised)
- D-1 PSK binder hash scope (commit 9a64055)
- C12 client-side multi-record reassembly via Handshake_Buffer

**Cryptographic primitives (50+ packages spot-checked; verifying
counterparts present):**
- ECDSA-P256 — both Sign + Verify present
- Ed25519 — both Sign + Verify present
- RSA-PSS — Verify only (FIPS 186-4 / RFC 8017 §8.1.2);
  Sign primitive missing (RFC 8017 §8.1.1) — Tier 3
- 3 production cipher suites declared (Chacha20-Poly1305-SHA256,
  AES-128-GCM-SHA256, AES-256-GCM-SHA384)
- All HACL\*-spec-port packages present (sha256/384/512, chacha20,
  poly1305, x25519, p256, rsa_pss verify, aes-128/256, ghash,
  hmac-sha256/384, hkdf, gcm_core)
- All bignum primitives (`bignum_2048` Mod_Exp / Mod_Mul present)

**Confirmed gaps (carried forward into wiring queue):**
- D-4 cert-mode merge (NEW finding — Tier P-RSA: RSA-PSS Sign
  primitive not implemented; ECDSA-P256 server-direction OK)
- C13 / C14 inbound auto-route (Step doesn't auto-demux post-Done
  Handshake records inside app-data flow)
- ALPN / SNI driver wiring (codec orphan — primitives exist in
  `Tls_Core.Extensions`, just unused)
- C12 server-side reassembly
- C10 broader alert wiring

## Process changes implemented after this audit

- CLAUDE.md §7 ("Interop-phase discipline: wire everything before
  testing anything") added.
- Audit-checklist requirement: every feature claim in a track must
  trace to all three of (a), (b), (c) above before being marked done.
